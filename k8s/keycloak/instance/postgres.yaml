---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rhdh-keycloak-postgres-pvc
  namespace: keycloak
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: rhdh-keycloak-postgres
  namespace: keycloak
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rhdh-keycloak-postgres
  template:
    metadata:
      labels:
        app: rhdh-keycloak-postgres
    spec:
      containers:
        - name: postgres
          image: registry.redhat.io/rhel9/postgresql-15:latest
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRESQL_USER
              valueFrom:
                secretKeyRef:
                  name: keycloak-db-secret
                  key: username
            - name: POSTGRESQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: keycloak-db-secret
                  key: password
            - name: POSTGRESQL_DATABASE
              value: keycloak
          volumeMounts:
            - name: data
              mountPath: /var/lib/pgsql/data
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: rhdh-keycloak-postgres-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: rhdh-keycloak-postgres
  namespace: keycloak
spec:
  selector:
    app: rhdh-keycloak-postgres
  ports:
    - port: 5432
      targetPort: 5432
