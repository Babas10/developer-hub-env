---
# Dynamic plugins configuration for Red Hat Developer Hub.
# Uses the default plugin set bundled with the RHDH image.
# Specific plugins will be enabled here once correct package references
# are confirmed for this RHDH version.
apiVersion: v1
kind: ConfigMap
metadata:
  name: dynamic-plugins-rhdh
  namespace: developer-hub
data:
  dynamic-plugins.yaml: |
    includes:
      - dynamic-plugins.default.yaml
    plugins:
      # ── Keycloak catalog sync ──────────────────────────────────────────────
      - package: ./dynamic-plugins/dist/backstage-community-plugin-catalog-backend-module-keycloak-dynamic
        disabled: false

      # ── Kubernetes (required by Tekton) ───────────────────────────────────
      - package: ./dynamic-plugins/dist/backstage-plugin-kubernetes
        disabled: false
      - package: ./dynamic-plugins/dist/backstage-plugin-kubernetes-backend-dynamic
        disabled: false

      # ── ArgoCD ────────────────────────────────────────────────────────────
      - package: oci://ghcr.io/redhat-developer/rhdh-plugin-export-overlays/backstage-community-plugin-argocd-backend:bs_1.49.4__1.4.0
        disabled: false
      - package: oci://ghcr.io/redhat-developer/rhdh-plugin-export-overlays/backstage-community-plugin-argocd:bs_1.49.4__2.8.0
        disabled: false
        pluginConfig:
          dynamicPlugins:
            frontend:
              backstage-community.plugin-argocd:
                translationResources:
                  - importName: argocdTranslations
                    module: Alpha
                    ref: argocdTranslationRef
                mountPoints:
                  - mountPoint: entity.page.overview/cards
                    importName: ArgocdDeploymentSummary
                    config:
                      layout:
                        gridColumnEnd:
                          lg: span 8
                          xs: span 12
                      if:
                        allOf:
                          - isArgocdConfigured
                  - mountPoint: entity.page.cd/cards
                    importName: ArgocdDeploymentLifecycle
                    config:
                      layout:
                        gridColumn: 1 / -1
                      if:
                        allOf:
                          - isArgocdConfigured

      # ── Metering (cost tracking) ──────────────────────────────────────────
      # OCI images built with plugins/build-oci.sh and pushed to quay.io/edubois10.
      # Repositories must be set to PUBLIC on quay.io so RHDH can pull without
      # cluster-level image pull secrets.
      - package: oci://quay.io/edubois10/rhdh-plugin-metering-backend:0.1.0!internal-backstage-plugin-metering-backend
        disabled: false

      - package: oci://quay.io/edubois10/rhdh-plugin-metering:0.1.0!internal-backstage-plugin-metering
        disabled: false
        pluginConfig:
          dynamicPlugins:
            frontend:
              internal.backstage-plugin-metering:
                apiFactories:
                  # Registers meteringApiRef — required because this plugin is
                  # wired via legacy mountPoints, so createFrontendPlugin's
                  # ApiBlueprint extensions are never processed by the app.
                  - importName: meteringApiFactory
                mountPoints:
                  - mountPoint: entity.page.overview/cards
                    importName: MeteringSummaryCard
                    config:
                      # Only show the cost card on entities that have the
                      # Kubernetes annotation — avoids a "missing annotations"
                      # message on every non-instrumented component.
                      if:
                        allOf:
                          - hasAnnotation: backstage.io/kubernetes-namespace
                      layout:
                        gridColumnEnd:
                          lg: span 4
                          md: span 6
                          xs: span 12
                  - mountPoint: entity.page.metering/cards
                    importName: MeteringTabContent
                    config:
                      layout:
                        gridColumn: 1 / -1
                entityTabs:
                  - path: /metering
                    title: Metering
                    mountPoint: entity.page.metering
                    if:
                      allOf:
                        - hasAnnotation: backstage.io/kubernetes-namespace


      - package: oci://ghcr.io/redhat-developer/rhdh-plugin-export-overlays/backstage-community-plugin-tekton:bs_1.49.4__3.37.0
        disabled: false
        pluginConfig:
          dynamicPlugins:
            frontend:
              backstage-community.plugin-tekton:
                translationResources:
                  - importName: tektonTranslations
                    ref: tektonTranslationRef
                mountPoints:
                  - mountPoint: entity.page.ci/cards
                    importName: TektonCI
                    config:
                      layout:
                        gridColumn: 1 / -1
                      if:
                        allOf:
                          - isTektonCIAvailable
