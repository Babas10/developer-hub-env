---
description: CRITICAL — Always use Red Hat registry images, never Docker Hub or community images
globs: **/*.yaml,**/*.yml,**/Containerfile,**/Dockerfile
alwaysApply: true
---

# Image Policy — CRITICAL

**Never use Docker Hub or community images** (`docker.io`, `bitnami`, `quay.io/bitnami`, etc.).

All container images must come from the Red Hat registry (`registry.redhat.io`).

## Approved Images

| Use case | Image |
|----------|-------|
| PostgreSQL 15 | `registry.redhat.io/rhel9/postgresql-15:latest` |
| PostgreSQL 16 | `registry.redhat.io/rhel9/postgresql-16:latest` |

When in doubt, search `registry.redhat.io` for the appropriate Red Hat supported image.

## Red Hat PostgreSQL Specifics

Red Hat images differ from community images:

- Env vars: `POSTGRESQL_USER`, `POSTGRESQL_PASSWORD`, `POSTGRESQL_DATABASE`
  (not `POSTGRES_USER` / `POSTGRES_PASSWORD` / `POSTGRES_DB`)
- Data directory: `/var/lib/pgsql/data` (not `/var/lib/postgresql/data`)
